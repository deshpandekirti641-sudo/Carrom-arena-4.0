'use client'

import { useState } from 'react'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Button } from '@/components/ui/button'
import { Badge } from '@/components/ui/badge'
import Link from 'next/link'

interface QuickStatProps {
  icon: string
  label: string
  value: string
  color?: string
}

function QuickStat({ icon, label, value, color = 'text-blue-600' }: QuickStatProps): JSX.Element {
  return (
    <div className="text-center">
      <div className="text-2xl mb-1">{icon}</div>
      <div className={`text-xl font-bold ${color}`}>{value}</div>
      <div className="text-xs text-gray-600">{label}</div>
    </div>
  )
}

export function ArenaMapOverview(): JSX.Element {
  const [isHovered, setIsHovered] = useState<boolean>(false)

  const quickStats = [
    { icon: '🎮', label: 'Gaming Tables', value: '50', color: 'text-blue-600' },
    { icon: '👥', label: 'Max Capacity', value: '1,003', color: 'text-green-600' },
    { icon: '🏆', label: 'Daily Prize Pool', value: '₹2.5Cr', color: 'text-yellow-600' },
    { icon: '⏱️', label: 'Avg Wait Time', value: '3min', color: 'text-purple-600' }
  ]

  const facilityHighlights = [
    { name: 'Main Gaming Hall', icon: '🎮', status: 'active', players: 180 },
    { name: 'VIP Lounge', icon: '👑', status: 'active', players: 32 },
    { name: 'Training Center', icon: '🎯', status: 'active', players: 65 },
    { name: 'Tournament Arena', icon: '🏆', status: 'maintenance', players: 0 }
  ]

  return (
    <div className="w-full space-y-6">
      {/* Header Section */}
      <Card className="bg-gradient-to-r from-blue-900 via-purple-900 to-indigo-900 text-white">
        <CardContent className="p-6">
          <div className="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-6">
            <div className="space-y-2">
              <h2 className="text-3xl font-bold">🏟️ Carrom Arena Building Map</h2>
              <p className="text-blue-100">
                Navigate India's premier real-money Carrom gaming facility with our interactive building map
              </p>
              <div className="flex flex-wrap gap-2 mt-3">
                <Badge variant="secondary" className="bg-white/20 text-white">
                  ✅ Live Navigation
                </Badge>
                <Badge variant="secondary" className="bg-white/20 text-white">
                  📊 Real-time Data
                </Badge>
                <Badge variant="secondary" className="bg-white/20 text-white">
                  🎯 Interactive Zones
                </Badge>
              </div>
            </div>
            
            <div className="flex flex-col items-center lg:items-end space-y-3">
              <Link href="/arena">
                <Button 
                  size="lg" 
                  className="bg-white text-blue-900 hover:bg-gray-100 font-semibold px-8"
                  onMouseEnter={() => setIsHovered(true)}
                  onMouseLeave={() => setIsHovered(false)}
                >
                  {isHovered ? '🧭 Explore Arena' : '🗺️ View Full Map'}
                </Button>
              </Link>
              <p className="text-xs text-blue-200">Click to access interactive navigation</p>
            </div>
          </div>
        </CardContent>
      </Card>

      {/* Quick Stats Grid */}
      <div className="grid grid-cols-2 lg:grid-cols-4 gap-4">
        {quickStats.map((stat, index) => (
          <Card key={index} className="hover:shadow-lg transition-shadow">
            <CardContent className="p-4">
              <QuickStat {...stat} />
            </CardContent>
          </Card>
        ))}
      </div>

      {/* Facility Status Overview */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        <Card>
          <CardHeader>
            <CardTitle className="flex items-center gap-2">
              📍 Live Facility Status
              <Badge variant="outline" className="bg-green-50 text-green-700">
                🟢 All Systems Online
              </Badge>
            </CardTitle>
          </CardHeader>
          <CardContent>
            <div className="space-y-3">
              {facilityHighlights.map((facility, index) => (
                <div key={index} className="flex items-center justify-between p-3 bg-gray-50 rounded-lg">
                  <div className="flex items-center gap-3">
                    <span className="text-xl">{facility.icon}</span>
                    <div>
                      <p className="font-medium text-gray-900">{facility.name}</p>
                      <p className="text-sm text-gray-600">
                        {facility.players > 0 ? `${facility.players} active players` : 'Currently closed'}
                      </p>
                    </div>
                  </div>
                  <Badge 
                    variant={facility.status === 'active' ? 'default' : 'secondary'}
                    className={
                      facility.status === 'active' 
                        ? 'bg-green-100 text-green-800' 
                        : 'bg-yellow-100 text-yellow-800'
                    }
                  >
                    {facility.status}
                  </Badge>
                </div>
              ))}
            </div>
            
            <div className="mt-4 pt-4 border-t">
              <Link href="/arena?tab=live-data">
                <Button variant="outline" className="w-full">
                  📊 View Detailed Live Data
                </Button>
              </Link>
            </div>
          </CardContent>
        </Card>

        <Card>
          <CardHeader>
            <CardTitle>🎯 Interactive Features</CardTitle>
          </CardHeader>
          <CardContent>
            <div className="space-y-4">
              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center flex-shrink-0">
                  <span className="text-sm">🧭</span>
                </div>
                <div>
                  <p className="font-medium text-gray-900">Step-by-Step Navigation</p>
                  <p className="text-sm text-gray-600">
                    Get turn-by-turn directions to any facility with estimated walking times
                  </p>
                </div>
              </div>
              
              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-green-100 rounded-full flex items-center justify-center flex-shrink-0">
                  <span className="text-sm">📊</span>
                </div>
                <div>
                  <p className="font-medium text-gray-900">Live Data Integration</p>
                  <p className="text-sm text-gray-600">
                    Real-time capacity, wait times, and prize pool information
                  </p>
                </div>
              </div>
              
              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-purple-100 rounded-full flex items-center justify-center flex-shrink-0">
                  <span className="text-sm">🎮</span>
                </div>
                <div>
                  <p className="font-medium text-gray-900">Multiple View Modes</p>
                  <p className="text-sm text-gray-600">
                    Floor plan, 3D view, and interactive zone selection
                  </p>
                </div>
              </div>
              
              <div className="flex items-start gap-3">
                <div className="w-8 h-8 bg-yellow-100 rounded-full flex items-center justify-center flex-shrink-0">
                  <span className="text-sm">📱</span>
                </div>
                <div>
                  <p className="font-medium text-gray-900">Mobile Optimized</p>
                  <p className="text-sm text-gray-600">
                    Touch-friendly interface with responsive design for all devices
                  </p>
                </div>
              </div>
            </div>
            
            <div className="mt-6 pt-4 border-t">
              <div className="grid grid-cols-2 gap-2">
                <Link href="/arena?tab=map&view=floor-plan">
                  <Button variant="outline" size="sm" className="w-full">
                    🏗️ Floor Plan
                  </Button>
                </Link>
                <Link href="/arena?tab=map&view=3d">
                  <Button variant="outline" size="sm" className="w-full">
                    🏢 3D View
                  </Button>
                </Link>
              </div>
            </div>
          </CardContent>
        </Card>
      </div>

      {/* Quick Actions */}
      <Card>
        <CardHeader>
          <CardTitle>⚡ Quick Arena Access</CardTitle>
        </CardHeader>
        <CardContent>
          <div className="grid grid-cols-2 md:grid-cols-4 gap-3">
            <Link href="/arena?zone=main-hall">
              <Button variant="outline" className="h-16 flex flex-col items-center justify-center gap-1 w-full">
                <span className="text-lg">🎮</span>
                <span className="text-xs">Gaming Hall</span>
              </Button>
            </Link>
            <Link href="/arena?zone=vip-lounge">
              <Button variant="outline" className="h-16 flex flex-col items-center justify-center gap-1 w-full">
                <span className="text-lg">👑</span>
                <span className="text-xs">VIP Lounge</span>
              </Button>
            </Link>
            <Link href="/arena?zone=wallet-center">
              <Button variant="outline" className="h-16 flex flex-col items-center justify-center gap-1 w-full">
                <span className="text-lg">💳</span>
                <span className="text-xs">Wallet Center</span>
              </Button>
            </Link>
            <Link href="/arena?zone=tournament-arena">
              <Button variant="outline" className="h-16 flex flex-col items-center justify-center gap-1 w-full">
                <span className="text-lg">🏆</span>
                <span className="text-xs">Tournament</span>
              </Button>
            </Link>
          </div>
          
          <div className="mt-4 text-center">
            <p className="text-sm text-gray-600 mb-3">
              Need help finding your way around? Our interactive map has you covered!
            </p>
            <Link href="/arena">
              <Button className="bg-gradient-to-r from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700 text-white font-semibold px-6">
                🗺️ Open Full Interactive Map
              </Button>
            </Link>
          </div>
        </CardContent>
      </Card>
    </div>
  )
}